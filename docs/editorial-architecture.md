# Редакционная архитектура портала ДИС

## Реализованный вертикальный срез

Публичный сайт и редакционная система используют одну базу, но разные правила
доступа. Публичные маршруты читают только материалы со статусом `published` и
заполненной датой публикации. Черновик открывается только авторизованному
администратору через отдельный маршрут предпросмотра с `noindex,nofollow`.

```text
Админка → черновик → предпросмотр → проверка полей → публикация
                                                   ↓
Главная → каталог материалов → статья → источники → связанные материалы
```

## Сущности

- `Article` — текст, раздел, тип, статус, автор, даты и медицинская проверка.
- `Rubric` — редакционная рубрика внутри одного из четырёх разделов.
- `Tag` — тема для фильтрации и связей.
- `ArticleSource` — название и исходный URL проверяемого источника.

Статьи не хранятся в `cases.py` и не смешиваются с карточками проектов.

## Безопасная публикация

Обычный материал нельзя опубликовать без заголовка, постоянного адреса,
описания, текста, раздела, рубрики, типа, автора и хотя бы одного тега.
Медицинский материал дополнительно требует:

1. хотя бы один источник;
2. медицинского редактора;
3. дату проверки;
4. публичное предупреждение о границах применения.

Life-OS может создавать только `draft`. Переход в `published`
выполняется отдельным действием администратора после проверки.

## Публичные маршруты

- `/materials/` — каталог и фильтры;
- `/materials/<slug>/` — опубликованная статья;
- `/admin/articles/` — редакционный список;
- `/admin/articles/<id>/preview/` — закрытый предпросмотр.

## Тематическая навигация и поиск

- 4 направления содержат 16 тематических страниц и 68 рубрик;
- карточка темы целиком кликабельна и имеет состояния hover и focus;
- рубрика ведёт в отфильтрованный каталог, а опубликованные материалы появляются на странице темы автоматически;
- `/search/` ищет только публичные материалы по заголовку, тексту, рубрике и тегам, а также проекты.

## Импорт Life-OS

- закрытая страница `/admin/imports/life-os/` принимает JSON до 250 КБ;
- импорт всегда создаёт `draft`, даже если пакет запрашивает публикацию;
- URL источников допускают только `http` и `https`;
- `package_id` и SHA-256 содержимого защищают от дублей и конфликта идентификаторов;
- публикация остаётся отдельным ручным действием с медицинскими проверками.

## Следующие этапы по ТЗ

1. Подписка с подтверждением адреса и выбором тем после запуска нового Telegram-бота.
2. Формирование коротких анонсов для Telegram, VK и email после публикации.
3. Миграции, резервное копирование и документированный откат.
4. Переход к PostgreSQL FTS после накопления корпуса; векторный слой — позднее.

Персональные рекомендации, кабинеты читателей, аудио и автоисследование не
входят в первый этап.

## План публикаций: долголетие (25 сентября 2026)

1. «От мечты о бессмертии к гипотезе воспалительного старения» — публикация через `data/content-packages/inflammaging-2026-09.json`, рубрика «Механизмы старения», 13 источников.
2. «IL-11: что показали опыты на мышах и что предстоит проверить на людях» — запланировано; проверить клинические исследования и безопасность.
3. «Как измерять здоровую продолжительность жизни» — запланировано; отделить клинические исходы от суррогатных биомаркеров.

Для всех материалов этой серии отделять бессмертие от здоровой продолжительности жизни, опыты на животных от исследований людей и не представлять гипотезы как медицинские рекомендации.
